<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 
    [템플릿 설명]  
    - 해당 파일은 SQL 문을 작성하는 곳입니다.
-->
<mapper namespace="com.kbhc.blackcode.Mapper.DataMapper">

    <select id="selectDataList" resultType="com.kbhc.blackcode.VO.DataVO">
        SELECT num,
               contents,
               date,
        FROM data;
    </select>
	
	<insert id="insertData" parameterType="com.kbhc.blackcode.VO.DataVO">

		INSERT INTO data(contents, date, rw)
		values (#{contents}, #{date}, #{rw});
		
	</insert>
	
	<select id="selectCountData" resultType="com.kbhc.blackcode.VO.DataInfoVO">
	
		SELECT
			(select count(*) from data where rw = 'W') as 'WRITE',
			(select count(*) from data where rw = 'R') as 'READ',
			count(*) as 'TOTAL'
		FROM data;
		
	</select>
	
</mapper>